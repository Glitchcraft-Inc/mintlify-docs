---
title: "Amplitude"
sidebarTitle: "Amplitude"
description: "Connect Amplitude to Actioneer as a data source."
---

# Linking Amplitude as a Source

This guide walks through connecting an Amplitude project to Actioneer. By the end, Actioneer will be able to query your event data and charts.

|  |  |
| :-- | :-- |
| **Prerequisites** | Amplitude account with project-level access, Actioneer account |
| **Access granted** | Event data and chart queries via the Amplitude API |
| **Estimated time** | 10 minutes |

<Info>
  Actioneer cannot create, modify, or delete any events, properties, or configuration in your Amplitude project.
</Info>

---

<Steps>
  <Step title="Find your Amplitude API credentials">
    Amplitude authenticates via project-level API keys. You need the **API Key** and **Secret Key** for the project you want Actioneer to query.

    1. Sign in at [app.amplitude.com](https://app.amplitude.com).
    2. Click your organization name in the top-left and select the target project.
    3. Go to **Settings** (gear icon) → **Projects** → select the project.
    4. Scroll to the **API Key** and **Secret Key** fields. Copy both.

    | Field | Location | Value Example |
    | :-- | :-- | :-- |
    | **API Key** | Project Settings | `abc123def456...` |
    | **Secret Key** | Project Settings | `sec789ghi012...` |

    <Warning>
      The Secret Key grants data export access. Treat it like a password — do not share it over unencrypted channels or commit it to version control.
    </Warning>
  </Step>
  <Step title="Connect Amplitude in Actioneer">
    1. In Actioneer, click **Data** in the left sidebar.
    2. Select **Amplitude** from the list of data sources.
    3. Complete the connection form:

    | Field | Value |
    | :-- | :-- |
    | **Connection Name** | A descriptive label, e.g. `Production Amplitude` |
    | **API Key** | From Step 1 |
    | **Secret Key** | From Step 1 |
    | **Data Center** | `US` (default) or `EU` — match your Amplitude project's data residency. If unsure, check **Settings → Projects → Server URL**. |

    4. Click **Connect**.
  </Step>
</Steps>

---

## Common questions

<AccordionGroup>
  <Accordion title="What Amplitude data can Actioneer access?">
    Actioneer can query event data, user properties, and charts accessible via the Amplitude API — the same data available to a project member. It cannot create, modify, or delete events, properties, or reports.
  </Accordion>

  <Accordion title="How does Actioneer protect credentials?">
    All credentials are encrypted with AES-256 at rest and transmitted over TLS 1.3 protocol. They are never stored in plain text or logged. Actioneer is SOC 2 Type II certified, ISO 27001 certified (audited by Schellman), and GDPR compliant.
  </Accordion>
</AccordionGroup>

<Card title="Need a hand?" icon="sparkles" horizontal>
  Stuck on a step or running into an error? Reach out to Actioneer support.
</Card>